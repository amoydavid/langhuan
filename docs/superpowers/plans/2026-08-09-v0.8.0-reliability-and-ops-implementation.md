# v0.8.0 可靠性与运维能力 — 实施清单

设计规格见 [`docs/superpowers/specs/2026-08-09-v0.8.0-reliability-and-ops-design.md`](../specs/2026-08-09-v0.8.0-reliability-and-ops-design.md)。

## 阶段1：配置与基础设施 ✅

- [x] `QueueConfig`（concurrency / retry.max_attempts / min/max_backoff / task_timeout / retention）+ helper 方法 + 默认值 + 校验
- [x] `ObservabilityConfig`（metrics.enabled/path、readiness.queue_pending_threshold）
- [x] `EmbeddingConfig`（batch_size / max_concurrency）
- [x] `IngestConfig.MaxChunksPerDocument`、`LogConfig.Redact`
- [x] `config.example.yaml` 补全所有新段（含中文注释）
- [x] 引入 `prometheus/client_golang` 依赖
- [x] 单测：`v080_reliability_test.go`（默认值 / YAML 加载 / 校验 / redact 默认 true / MaxRetry 边界）

## 阶段2：asynq 任务治理 ✅

- [x] 入队侧：`JobRequest` 加 MaxRetry/Timeout/Retention 可选字段；`QueueDefaults` 注入；`NewQueueWithDefaults`
- [x] Server 侧：`asynqServerConfig`（Concurrency / RetryDelayFunc / slog adapter / ErrorHandler）
- [x] `QueueInspector` 封装（Snapshots / TotalPending / ListDead / RetryDead / DeleteDead）
- [x] main.go 装配：`NewQueueWithDefaults` + Inspector 创建 + cleanup
- [x] 单测：入队 opts 注入（默认/覆盖/零值）、Inspector 全方法、retryDelayFunc 退避曲线

## 阶段3：失败重试 ✅

- [x] `ErrNotRetryable` 错误 + HTTP 409 `not_retryable` 映射
- [x] `DocumentRetryService`（RetryDocument / RetryJob，事务内定位+复位 → 事务外入队，稳定 TaskID 幂等）
- [x] `DocumentRetryDBStore`（GetLatestRevision / GetJobRevision / ResetFailedRevision：failed→pending，复位/新建 parse job，document→pending）
- [x] REST：`POST /documents/:id/retry`、`POST /jobs/:id/retry`（admin/owner）
- [x] MCP：`document_retry` 工具（含 scope 契约）
- [x] main.go DI 装配
- [x] 测试：service 单测（7）、DB 集成测试（5，含幂等/跨 workspace 404/非 failed 拒绝）

## 阶段4：Generation Reindex ✅

- [x] `IndexGenerationService.Reindex`（薄封装 Create，配置全 nil 继承 active base）
- [x] REST：`POST /knowledge-bases/:id/reindex`（admin）
- [x] HTTP 错误映射复用 `ErrGenerationBuildInProgress`
- [x] 测试：handler fake 加 Reindex 方法，路由契约测试含 reindex 路由

## 阶段5：可观测性 ✅

- [x] logger 脱敏：`ReplaceAttr` 对 authorization/bearer/api_key/token/password/secret/dsn + Bearer/lhk_ 前缀 mask；`log.redact` 默认 true
- [x] 统一日志字段约定（spec 6.1，`logger/attrs.go` 设计——实际复用各模块现有 attrs）
- [x] `/readyz`：ping DB + Redis + 队列积压阈值；503 on failure
- [x] Prometheus metrics：HTTP 请求、导入阶段、embedding、队列 gauge；`/metrics` 端点
- [x] HTTP 指标中间件（`MetricsMiddleware`）
- [x] 导入阶段耗时打点：worker RunParse/RunChunk/RunIndex 前后 `recordStage`（StageRecorder 接口）
- [x] 队列管理端点：`GET /admin/queues`、`GET /admin/queues/:queue/dead`、`POST .../dead/:task_id/retry`、`DELETE .../dead/:task_id`（platform admin）
- [x] `QueueAdminService` + `inspectorPortAdapter`（asynq→service 类型转换，保持依赖方向）
- [x] main.go 装配（metrics 实例 + readiness checker + queue admin）
- [x] 测试：logger 脱敏（12）

## 阶段6：资源保护 ✅

- [x] chunk 数量限制：`ChunkStage.WithMaxChunksPerDocument`，超限 terminal ErrValidation
- [x] embedding 并发上限：`IndexStage.WithEmbeddingLimits`，errgroup + semaphore 并发执行 batch（`embedding.max_concurrency` 真实生效）
- [x] embedding batch 上限：`EmbeddingBatchLimit` 与模型级 batch 取 min（`embedding.batch_size` 真实生效）
- [x] `DocumentPipelineDeps` 透传 MaxChunksPerDocument / EmbeddingConcurrency / EmbeddingBatchLimit
- [x] retrieval cleanup 定时调度：`CleanupGlobal`（跨 workspace 批量删 staging/failed/retired）+ `CleanupIntervalSeconds` 配置 + worker 进程定时 goroutine
- [x] 测试：chunk 超限单测、embedding 并发顺序对齐单测、batch limit 覆盖单测、retrieval cleanup fake 加 CleanupGlobal

## 阶段7：Web Console ✅

- [x] API：`retryDocument`（documents/api.ts）、`reindexKnowledgeBase`（index-generations/api.ts）
- [x] 文档详情页：failed 状态显示 "重试" 按钮 + mutation + invalidate
- [x] Indexes 页："重建索引" 按钮 + mutation + toast
- [x] i18n：zh/en 补全 retryButton / retryStartedToast / reindexButton / reindexStartedToast
- [x] `pnpm check` / `pnpm test`（331）/ `pnpm build` 全通过
- [ ] 队列监控页（后端 API 已就绪，前端页留作后续增强）

## 阶段8：备份恢复 ✅

- [x] `docs/operations/backup-restore.md`：数据库 / 对象存储 / 配置 / encryption_key 的备份恢复 runbook + smoke 流程

## 验收对照（ROADMAP v0.8.0）

- [x] 可定位失败发生在哪个阶段并关联 lineage（阶段耗时 metrics + 统一日志 attrs）
- [x] 大文件/压缩炸弹/慢 Provider/积压不无限占用 worker（task timeout + concurrency + chunk 限制 + embedding 并发/batch 限制）
- [x] 失败文档可安全重试，重复请求和 worker 重启不破坏 active Generation（幂等重试 + 双缓冲 reindex）
- [x] 空库可重复完成安装/备份/恢复/smoke（runbook）
- [x] go test / vet / gofmt + 集成测试 + pnpm check/test/build 通过

## 安全评审修复（评审追加）

- [x] 后端权限漏洞修复：`/documents/:id/retry`、`/jobs/:id/retry` 加 `RequireAdminForSession()`（原挂在 member 组，member 可越权）
- [x] 前端 document-detail.tsx retry 按钮加 `canManageIndex` 权限守卫（原仅判 failed 状态）
- [x] service 层注释修正（与实际中间件一致）
- [x] e2e 负向测试：member 调 retry/reindex 三端点均 403（`TestV080RetryReindexPermissionE2E`）

## 实现完整性审计修复（审计追加）

- [x] embedding 并发接入：`index_stage.embedIndexTexts` 支持 errgroup + semaphore 并发（`EmbeddingConcurrency` 真实生效，原为死配置）
- [x] embedding batch 接入：`EmbeddingBatchLimit` 与模型级 batch 取 min（`embedding.batch_size` 真实生效，原为死配置）
- [x] 移除死指标：`SetQueueSize`/`ObserveEmbeddingCall`/`NoopRecorder` 从未被调用，移除避免假指标误导监控
- [x] 创建 `logger/attrs.go` 统一字段约定（`LineageAttrs`/`StageAttrs` helper + 字段名常量）

## 后续增强（不在 v0.8.0 硬性范围）

- Web Console 队列监控页（后端 `/admin/queues/*` 已就绪，前端页待实现）
- 队列 gauge 周期采集（需周期 goroutine 调 Inspector 推 `langhuan_asynq_queue_size`；当前 gauge 已移除，等接入时连同指标一起补）
- embedding 调用计数指标（`langhuan_embedding_calls_total` 已移除，等接入 embedding 调用点时一起补）
