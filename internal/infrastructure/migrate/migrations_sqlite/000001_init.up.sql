-- 占位迁移：验证 SQLite 迁移管线通畅（golang-migrate database/sqlite 纯 Go 驱动）。
-- 实际 schema 在切片 3 填充（core_schema / knowledge_and_jobs / retrieval_and_search_runs / fts_projection）。
-- 切片 3 会用真实 schema 替换本文件。
SELECT 1;
